"use strict";
describe('Data Type', () => {
    it("Should must declare", () => {
        const name = "j anna";
        const balance = 10000;
        const isVip = true;
        console.log(name);
        console.log(balance);
        console.log(isVip);
    });
});
